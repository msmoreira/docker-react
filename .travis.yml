language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t msmoreira/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true msmoreira/docker-react npm run test -- --coverage

